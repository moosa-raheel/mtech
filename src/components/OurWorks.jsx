import css from "../css/style.module.css";
export default function OurWorks() {
  return (
    <h4
      className={`${css.f_40} ${css.text_center} ${css.l_black}`}
      style={{ margin: "3rem 0" }}
    >
      Our work is spread all over the world
    </h4>
  );
}
